import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'rettich.dumpster.diving',
  appName: 'dumpster-diving-ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
